<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/advance_setting_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- 用现代 edge-to-edge（WindowCompat）控制沉浸式，避免旧 windowTranslucent* 产生火色蒙层 -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!--
        Splash launch theme (compat).
        On API < 31 this uses AndroidX core-splashscreen backport: shows a solid
        background and centered launcher icon, then jumps to postSplashScreenTheme.
        On API >= 31 it is overridden in values-v31/styles.xml to use the system
        SplashScreen API with animated icon support.
    -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <!-- Transparent streaming background to avoid extra overdraw -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!-- StreamSettings Activity 的 AppCompat 主题（AndroidX PreferenceFragmentCompat 需要） -->
    <style name="SettingsThemeCompat" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/advance_setting_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- 沉浸式顶栏：内容延伸到状态栏后方，由 Activity 通过 WindowCompat.setDecorFitsSystemWindows(false) 控制；
             禁用旧 windowTranslucent* 标志以避免系统自动添加灰色 scrim 蒙层 -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="preferenceTheme">@style/MyPreferenceThemeOverlay</item>
    </style>

    <!-- 自定义 Preference 主题：紧凑左缩进 (无 icon 不预留空间) -->
    <style name="MyPreferenceThemeOverlay" parent="@style/PreferenceThemeOverlay">
        <item name="preferenceStyle">@style/MyPreferenceCompact</item>
        <item name="checkBoxPreferenceStyle">@style/MyCheckBoxPreferenceCompact</item>
        <item name="switchPreferenceStyle">@style/MySwitchPreferenceCompact</item>
        <item name="switchPreferenceCompatStyle">@style/MySwitchPreferenceCompact</item>
        <item name="dialogPreferenceStyle">@style/MyDialogPreferenceCompact</item>
        <item name="editTextPreferenceStyle">@style/MyEditTextPreferenceCompact</item>
        <item name="dropdownPreferenceStyle">@style/MyDropDownPreferenceCompact</item>
    </style>

    <style name="MyPreferenceCompact" parent="@style/Preference.Material">
        <item name="iconSpaceReserved">false</item>
    </style>
    <style name="MyCheckBoxPreferenceCompact" parent="@style/Preference.CheckBoxPreference.Material">
        <item name="iconSpaceReserved">false</item>
    </style>
    <style name="MySwitchPreferenceCompact" parent="@style/Preference.SwitchPreferenceCompat.Material">
        <item name="iconSpaceReserved">false</item>
    </style>
    <style name="MyDialogPreferenceCompact" parent="@style/Preference.DialogPreference.Material">
        <item name="iconSpaceReserved">false</item>
    </style>
    <style name="MyEditTextPreferenceCompact" parent="@style/Preference.DialogPreference.EditTextPreference.Material">
        <item name="iconSpaceReserved">false</item>
    </style>
    <style name="MyDropDownPreferenceCompact" parent="@style/Preference.DropDown.Material">
        <item name="iconSpaceReserved">false</item>
    </style>

    <!-- 通用弹窗样式 -->
    <style name="AppDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/app_dialog_bg_cute</item>
        <item name="android:windowMinWidthMajor">60%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/AppDialogAnimation</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <!-- 专业标题颜色设置 -->
        <item name="android:textColorPrimary">@color/app_dialog_title_color</item>
        <item name="android:textColorSecondary">@color/app_dialog_subtitle_color</item>
        <item name="android:colorAccent">@color/app_dialog_accent_color</item>
        <item name="android:listDivider">@color/app_dialog_accent_color</item>
        <!-- 标题样式 -->
        <item name="android:windowTitleStyle">@style/AppDialogTitleStyle</item>
    </style>

    <!-- 通用弹窗动画 -->
    <style name="AppDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/game_menu_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/game_menu_dialog_exit</item>
    </style>

    <!-- 通用对话框标题样式 -->
    <style name="AppDialogTitleStyle" parent="@style/RtlOverlay.DialogWindowTitle.AppCompat">
        <item name="android:textColor">@color/app_dialog_title_color</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <!-- 进度对话框样式 -->
    <style name="AppProgressDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/game_menu_dialog_bg</item>
        <item name="android:windowMinWidthMajor">50%</item>
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/AppDialogAnimation</item>
        <item name="android:textColorPrimary">@color/theme_pink_primary</item>
        <item name="android:colorAccent">@color/theme_pink_primary</item>
        <item name="android:progressTint">@color/theme_pink_primary</item>
        <item name="android:indeterminateTint">@color/theme_pink_primary</item>
        <item name="android:indeterminateDrawable">@drawable/app_progress_indeterminate</item>
    </style>

    <!-- 专门用于 iPerf3 对话框的样式 -->
    <style name="Iperf3DialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <!-- 对话框窗口背景色 (这是整个窗口的背景，包括标题和按钮区域) -->
        <item name="android:background">#1C1C1E</item>
        <!-- 标题文字样式 -->
        <item name="android:windowTitleStyle">@style/Iperf3DialogTitleText</item>
        <!-- 内容区域文字颜色 (虽然我们自定义了布局，但设置一下没坏处) -->
        <item name="android:textColor">#FFFFFF</item>
        <!-- 标题文字颜色 (更通用的方式) -->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <!-- 次要文字颜色 -->
        <item name="android:textColorSecondary">#CCCCCC</item>
        <!-- 按钮样式 -->
        <item name="buttonBarPositiveButtonStyle">@style/Iperf3DialogButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/Iperf3DialogButton</item>
        <item name="buttonBarNeutralButtonStyle">@style/Iperf3DialogButton</item>
        <!-- 按钮颜色 (这是控制按钮文字颜色的关键属性) -->
        <item name="colorAccent">#007AFF</item>
    </style>
    <!-- 自定义标题文字样式 -->
    <style name="Iperf3DialogTitleText" parent="@style/RtlOverlay.DialogWindowTitle.AppCompat">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textStyle">bold</item>
    </style>
    <!-- 自定义按钮样式 (可以进一步定制，比如背景等) -->
    <style name="Iperf3DialogButton" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <!-- 按钮文字颜色会继承自上面定义的 colorAccent -->
        <item name="android:textColor">#007AFF</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
    </style>



</resources>
